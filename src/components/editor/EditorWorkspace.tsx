'use client';
import Editor from '@monaco-editor/react'; import { useWorkspaceStore } from '@/stores/useWorkspaceStore'; import { EditorCommandStrip } from './EditorCommandStrip';
export function EditorWorkspace(){const s=useWorkspaceStore(); const paths=Object.keys(s.files); return <section className="flex h-full min-w-0 flex-col bg-slate-900"><div className="flex h-10 gap-1 overflow-x-auto border-b border-white/10 px-2 pt-2">{paths.map(p=><button key={p} onClick={()=>s.setActiveFile(p)} className={`rounded-t-xl px-3 text-xs ${p===s.activeFile?'bg-slate-800 text-white':'bg-slate-950 text-slate-400'}`}>{p}</button>)}</div><div className="min-h-0 flex-1"><Editor theme="vs-dark" path={s.activeFile} value={s.files[s.activeFile] ?? ''} onChange={v=>s.updateFile(s.activeFile,v ?? '')} options={{minimap:{enabled:false},fontSize:13,wordWrap:'on',automaticLayout:true}} /></div><EditorCommandStrip/></section>}

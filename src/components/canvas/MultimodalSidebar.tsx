'use client';
import { UploadCloud } from 'lucide-react'; import { useWorkspaceStore } from '@/stores/useWorkspaceStore';
export function MultimodalSidebar(){const s=useWorkspaceStore(); const ws=s.workspaces.find(w=>w.id===s.activeId)!; return <aside className="h-full overflow-y-auto bg-slate-950 p-4 scrollbar-thin"><div className="rounded-3xl border-2 border-dashed border-slate-700 p-6 text-center"><UploadCloud className="mx-auto text-indigo-300"/><b>Drop sketch, wireframe, diagram, or telemetry chart</b><p className="mt-2 text-sm text-slate-400">Vision decomposition streams below.</p></div><div className="mt-4 flex flex-wrap gap-2">{ws.templateTags.map(t=><span key={t} className="rounded-full bg-slate-800 px-3 py-1 text-xs">{t}</span>)}</div><details className="mt-5 rounded-2xl bg-slate-900 p-4" open><summary className="cursor-pointer font-bold">Vision telemetry</summary>{s.telemetry.map(t=><div key={t.label} className="mt-3"><div className="flex justify-between text-sm"><span>{t.label}</span><span>{Math.round(t.confidence*100)}%</span></div><p className="text-xs text-slate-400">{t.detail}</p></div>)}</details><p className="mt-4 text-sm text-slate-400">{ws.description}</p></aside>}

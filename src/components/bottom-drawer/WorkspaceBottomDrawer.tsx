'use client';
import { useWorkspaceStore } from '@/stores/useWorkspaceStore'; import { TerminalView } from './TerminalView'; import { DiagnosticStreamView } from './DiagnosticStreamView'; import { FlamegraphView } from './FlamegraphView'; import { OpenApiView } from './OpenApiView';
const heights={collapsed:'40px',default:'280px',expanded:'500px',fullscreen:'calc(100vh - 64px)'};
export function WorkspaceBottomDrawer(){const s=useWorkspaceStore(); const view={terminal:<TerminalView/>,healing:<DiagnosticStreamView/>,flamegraph:<FlamegraphView/>,openapi:<OpenApiView/>}[s.drawerTab]; return <section className="border-t border-white/10 bg-slate-900" style={{height:heights[s.drawerHeight]}}><div className="flex h-10 items-center gap-2 px-3">{(['terminal','healing','flamegraph','openapi'] as const).map(t=><button key={t} onClick={()=>s.setDrawer(t)} className={`rounded-lg px-3 py-1 text-xs ${s.drawerTab===t?'bg-indigo-500':'bg-slate-800'}`}>{t}</button>)}<div className="ml-auto flex gap-1">{(['collapsed','default','expanded','fullscreen'] as const).map(h=><button key={h} onClick={()=>s.setDrawer(s.drawerTab,h)} className="rounded bg-slate-800 px-2 py-1 text-xs">{h}</button>)}</div></div><div className="h-[calc(100%-40px)]">{s.drawerHeight!=='collapsed' && view}</div></section>}
